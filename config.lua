-- Keep in note this will remove all the items in each inventory it selects.

Config = {
    RemoveItems = { -- Add it new items to remove
        'water',
        'WEAPON_PISTOL',
        'burger'
    }
}